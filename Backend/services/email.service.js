export const sendEmail = async ({ to, subject, text }) => { const { RESEND_API_KEY, EMAIL_FROM } = process.env; if (!RESEND_API_KEY || !EMAIL_FROM) throw Object.assign(new Error("Email is not configured"), { status: 503 }); const response = await fetch("https://api.resend.com/emails", { method: "POST", headers: { Authorization: `Bearer ${RESEND_API_KEY}`, "Content-Type": "application/json" }, body: JSON.stringify({ from: EMAIL_FROM, to: [to], subject, text }) }); if (!response.ok) throw Object.assign(new Error("Unable to send email"), { status: response.status === 429 ? 429 : 502 }); };
