import express from "express";import{authenticateToken}from"../middleware/auth.middleware.js";import{getMine,listMine,updateStatus,withdraw}from"../controllers/application.controllers.js";const r=express.Router();r.use(authenticateToken);r.get("/",listMine);r.get("/:applicationId",getMine);r.put("/:applicationId/withdraw",withdraw);r.put("/:applicationId/status",updateStatus);export default r;
