import express from "express"; import { authenticateToken } from "../middleware/auth.middleware.js"; import { closeMyOpportunity, createMyOpportunity, deleteMyOpportunity, getOpportunity, listOpportunities, publishMyOpportunity, updateMyOpportunity } from "../controllers/opportunity.controllers.js";
const router=express.Router(); router.use(authenticateToken); router.get("/",listOpportunities); router.get("/:opportunityId",getOpportunity); router.post("/",createMyOpportunity); router.put("/:opportunityId",updateMyOpportunity); router.delete("/:opportunityId",deleteMyOpportunity); router.post("/:opportunityId/publish",publishMyOpportunity); router.post("/:opportunityId/close",closeMyOpportunity); export default router;
