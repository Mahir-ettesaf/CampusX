import {findAdminUser,listAdminUsers} from "../models/admin-user.model.js";const roles=new Set(["student","graduate","faculty","recruiter","admin"]),id=v=>{const n=Number(v);return Number.isInteger(n)&&n>0?n:null},fail=(r,s,m)=>r.status(s).json({success:false,message:m});export const listUsers=async(req,res)=>{if(req.user.role!=="admin")return fail(res,403,"Administrator access is required");const search=typeof req.query.search==="string"?req.query.search.trim():"",role=typeof req.query.role==="string"?req.query.role.trim():"";if(search.length>150)return fail(res,400,"Search text is too long");if(role&&!roles.has(role))return fail(res,400,"Provide a valid role filter");try{return res.json({success:true,users:await listAdminUsers({search,role})})}catch{return fail(res,500,"Unable to load users")}};export const getUser=async(req,res)=>{if(req.user.role!=="admin")return fail(res,403,"Administrator access is required");const userId=id(req.params.userId);if(!userId)return fail(res,400,"Provide a valid user ID");try{const user=await findAdminUser(userId);return user?res.json({success:true,user}):fail(res,404,"User was not found")}catch{return fail(res,500,"Unable to load user")}};
