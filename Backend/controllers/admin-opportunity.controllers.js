import{findAdminOpportunity,listAdminOpportunities as list}from"../models/admin-opportunity.model.js";const types=new Set(["internship","job","ra","ta","research"]),states=new Set(["draft","published","closed"]),id=v=>{const n=Number(v);return Number.isInteger(n)&&n>0?n:null},fail=(r,s,m)=>r.status(s).json({success:false,message:m});export const listAdminOpportunities=async(req,res)=>{if(req.user.role!=="admin")return fail(res,403,"Administrator access is required");const{search="",opportunity_type="",status=""}=req.query;if(String(search).length>150||opportunity_type&&!types.has(String(opportunity_type))||status&&!states.has(String(status)))return fail(res,400,"Provide valid opportunity filters");try{return res.json({success:true,opportunities:await list({search:String(search).trim(),type:String(opportunity_type),status:String(status)})})}catch{return fail(res,500,"Unable to load opportunities")}};export const getAdminOpportunity=async(req,res)=>{if(req.user.role!=="admin")return fail(res,403,"Administrator access is required");const i=id(req.params.opportunityId);if(!i)return fail(res,400,"Provide a valid opportunity ID");try{const opportunity=await findAdminOpportunity(i);return opportunity?res.json({success:true,opportunity}):fail(res,404,"Opportunity was not found")}catch{return fail(res,500,"Unable to load opportunity")}};
