import{useEffect,useState}from"react";import{ActivityIndicator,ScrollView,Text,TouchableOpacity,View}from"react-native";import{useNavigation,useRoute}from"@react-navigation/native";import{getAdminUser,AdminUserDetail,adminUserError}from"../../services/admin-user.service";export default function AdminUserDetailsScreen(){const n=useNavigation<any>(),r=useRoute<any>(),[u,setU]=useState<AdminUserDetail|null>(null),[e,setE]=useState("");const load=async()=>{try{setU(await getAdminUser(Number(r.params?.userId)))}catch(x){setE(adminUserError(x))}};useEffect(()=>{void load()},[]);if(!u)return <View><Text>{e||"Loading user…"}</Text><TouchableOpacity onPress={()=>void load()}><Text>Retry</Text></TouchableOpacity></View>;return <ScrollView contentContainerStyle={{padding:24}}><TouchableOpacity onPress={()=>n.goBack()}><Text>Back to Users</Text></TouchableOpacity><Text style={{fontSize:28,fontWeight:"bold"}}>{u.full_name}</Text><Text>{u.email}</Text><Text>{u.role}</Text><Text>Created: {u.created_at}</Text><Text style={{marginTop:20,fontWeight:"bold"}}>Related records</Text>{[["Skills",u.skills_count],["Resumes",u.resumes_count],["Certificates",u.certificates_count],["Projects",u.projects_count],["Publications",u.publications_count],["Applications",u.applications_count],["Opportunities",u.opportunities_count]].map(([x,y])=><Text key={String(x)}>{x}: {y}</Text>)}</ScrollView>}
